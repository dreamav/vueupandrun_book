const userMixin = {
    methods: {
        getUserInformation(userId) {
        	let user = [
        		'ddd',
        		'2svsv',
        		'3sdvsd'
        	];
            return user[userId];
        }
    }
}